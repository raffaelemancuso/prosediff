[project]
name = "prosediff"
version = "0.1.0"
description = "Side-by-side diff for prose, not code: optimised for Word .docx documents, Markdown included, from two files or from git"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["diff", "docx", "word", "markdown", "prose", "compare", "track changes"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Text Processing",
]
authors = [
    { name = "Raffaele Mancuso", email = "raffaelemancuso532@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "gitpython>=3.1.62",
    "jinja2>=3.1.6",
    "markupsafe>=3.0",
    "python-docx>=1.2.0",
    "rapidfuzz>=3.14.6",
    "yasbd-lib>=1.0.0,<2",
]

[project.urls]
Homepage = "https://github.com/raffaelemancuso/prosediff"
Issues = "https://github.com/raffaelemancuso/prosediff/issues"

[project.scripts]
prosediff = "prosediff.cli:main"

# A window, without a console on Windows.
[project.gui-scripts]
prosediff-gui = "prosediff.gui:main"

[dependency-groups]
dev = [
    "playwright>=1.63.0",
    "pytest>=8.4.0",
    "ruff>=0.16.9",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF"]
ignore = [
    "RUF001",  # ambiguous characters: the page uses − and … on purpose
    "RUF002",
    "RUF003",
]
